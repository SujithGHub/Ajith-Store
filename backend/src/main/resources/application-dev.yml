spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/ajith_store_dev
    username: ajith_user
    password: ajith_pass

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.ajith.store: DEBUG
    org.springframework.security: DEBUG
